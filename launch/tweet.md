# Launch tweet (X/Twitter)

**From @mcpindex:**

shipping mcpindex.ai

an agent-native index of MCP servers — recommendation API + drop-in MCP server for Claude Desktop, Cursor, Cline, Zed

3,500+ servers indexed daily from the official registry
ranked by an open quality score
free tier, 60 req/min, no key

→ https://mcpindex.ai

**Reply tweet (thread):**

`npm install -g mcp-server-mcpindex`

now your agent can find MCP servers from inside the agent loop. ask claude "find me an mcp for postgres with read-only mode" and it returns ranked picks with install commands

source on github · MIT for the npm package · unofficial

**Personal handle (different angle):**

worked on this between meetings for the last week — mcpindex.ai

an MCP server for finding MCP servers (yes, recursive). built so the IDE can find the right server at inference time, not the dev scrolling a sidebar

3 endpoints, one npm package, daily snapshot — https://mcpindex.ai
