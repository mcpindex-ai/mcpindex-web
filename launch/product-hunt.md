# Product Hunt launch

**Tagline (60 chars):**
> The agent-native index of MCP servers

**Description (260 chars):**
> Find the right MCP server at inference time. Recommendation API + drop-in MCP server (`npm install -g mcp-server-mcpindex`) for Claude Desktop, Cursor, Cline, Zed. 3,500+ servers indexed daily from the official registry, ranked by open Quality Score.

**First comment (post yourself within 5 min of launch):**

Hey hunters — I built this because every existing MCP directory is built for humans browsing. None work well as the API an IDE calls when a user types `/add mcp postgres`.

Three things on the page:

1. Live demo. Type a task, get top 3 ranked picks with install commands. Free.
2. An MCP server that finds other MCP servers — install once, never browse a sidebar again.
3. Open Quality Score methodology — five public-data signals, source on GitHub.

3,500+ servers indexed daily. Free tier 60 req/min/IP. Unofficial — not affiliated with Anthropic.

What would make it useful enough to leave installed? Genuinely curious.

**Topics:** Developer Tools, Open Source, AI

**Schedule:** Tuesday following the Show HN (avoid same-day distribution overlap)
