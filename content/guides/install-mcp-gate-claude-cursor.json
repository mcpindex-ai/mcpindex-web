{
  "slug": "install-mcp-gate-claude-cursor",
  "title": "Install the mcpindex gate in Claude Desktop, Cursor, Cline, or Zed",
  "meta_description": "One-command and auditable installs for the mcpindex in-path drift gate across Claude Desktop, Cursor, Cline, and Zed — what it does and what it does not claim.",
  "h1": "Install the mcpindex gate in Claude, Cursor, Cline, or Zed",
  "body": "The mcpindex gate sits in your MCP host session so each configured server launches behind a contract pin. On first sight it records the tool contract. On later calls it diffs the live contract and HOLDs when a silent breaking change appears — before the agent acts on the new shape.\n\nConvenience install (inspect first):\n\ncurl -fsSL https://mcpindex.ai/install.sh | less\n\nThen run the same URL piped to sh, or follow the auditable path in the docs: uv tool install mcpindex-preflight plus the host-config wizard. The script rewrites MCP host config only; uninstall.sh restores it. No API key. The gate does not take custody of your provider tokens.\n\nAfter install, restart the host. When a tool drifts, you get a HOLD with a ChangeKind — review, re-pin, or validate. The blast-radius grade (read/write/delete/send, reversible or not) is advisory and never overrides HOLD/PROCEED.\n\nThis is not a malware scanner and not a guarantee the tool is safe. It is a deterministic contract-diff in the path. For discovery and advisory screens without the gate, use the directory client (npm install -g mcp-server-mcpindex) or the HTTP API.\n\nFull wiring notes: https://mcpindex.ai/docs#install-the-gate",
  "citation_ids": [
    "mcpindex-snapshot:io-github-nirholas-3d-agent-mcp",
    "mcpindex-snapshot:io-github-br0ski777-pdf-generator"
  ],
  "project": "mcpindex",
  "updated": "2026-07-12"
}
